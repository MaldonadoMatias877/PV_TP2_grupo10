// ejercicio5.js - Conversión de horas/minutos a segundos
const horas = 2;
const minutos = 30;
const segundos = (horas * 3600) + (minutos * 60);

console.log(`${horas}h ${minutos}min = ${segundos} segundos`);